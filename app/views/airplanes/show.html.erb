<!-- NAVBAR -->
<div class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid justify-content-between">
    <%= link_to "Fly Me Now", airplanes_path, class:"navbar-brand text-dark" %>
    <form class="form-inline my-2 my-lg-0" action="/airplanes">
      <input class="form-control mr-sm-2" type="search" placeholder="Search" aria-label="Search" name="model" value="<%= params[:model] %>">
      <button class="btn btn-outline-success my-2 my-sm-0" type="submit">Search</button>
      <% if policy(Airplane).create? %>
        <button class="btn btn-outline-primary my-2 my-sm-0 mx-2"><%= link_to "Add Airplane", new_airplane_path %></button>
      <% end %>
    </form>
  </div>
</div>
<!-- MAIN CONTENT -->
<% if @airplane.photos.attached? %>
  <div class="banner" id="banner-show" style="height: 350px; background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url(<%= cl_image_path @airplane.photos.first.key %>); background-size: cover; background-position: center;">
<% else %>
    <div class="banner" id="banner-show" style="height: 350px; background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url(https://images.unsplash.com/photo-1474302770737-173ee21bab63?ixlib); background-size: cover; background-position: center;">
<% end %>
  <div class="container">
    <p id="notice"><%= notice %></p>
    <div class="row" id="grid-show">
      <div class="col-lg-8">
        <div class="container bg-light p-4 rounded-lg my-3">
          <h1><%= @airplane.model %></h1>
          <p>Model</p>
        </div>
        <div class="container bg-light p-4 rounded-lg">
          <h3>A bit more about this airplane...</h3>
          <!-- <p>LOCATION PENDING GEOPLOACTAION<#%= @airplane.location %></p> TODO: add the airplane location GEOLOCATING-->
        </div>
        <% if policy(Airplane).update? %>
          <div class="container bg-light p-4 rounded-lg my-3">
            <h3>Bookings:</h3>
            <% @airplane.bookings.each do |booking| %>
              <!-- TODO -->
              <!-- List airplane's bookings -->
            <% end %>
          </div>
        <% end %>
      </div>
      <div class="col-lg-4">
        <% if current_user.role == "renter" %>
          <div class="container bg-light p-4 rounded-lg mt-3">
            <h6>Book this plane!</h6>
            <%= simple_form_for [@airplane,@booking] do |f| %>
              <%= f.input :start_time %>
              <%= f.input :end_time %>
              <%= f.submit %>
            <% end %>
          </div>
        <% end %>
        <div class="container bg-light p-4 rounded-lg mt-3">
          <h3>Details</h3>
          <h6>
            <i class="fas fa-user-friends"><%= @airplane.capacity %></i>
            <i class="fas fa-euro-sign px-2"><%= @airplane.price %></i>
            <i class="fas fa-star"><%= @airplane.rating %></i>
          </h6>
        </div>
        <div class="container-fluid justify-content-between bg-light p-4 rounded-lg mt-3">
          <p>Share</p>
          <h3>
            <i class="fab fa-facebook-square"></i>
            <i class="fab fa-twitter-square"></i>
            <i class="fab fa-pinterest-square"></i>
            <i class="fas fa-envelope-square"></i>
          </h3>
        </div>
      </div>
    </div>
  </div>
</div>

